server:
  port: ${SERVER_PORT:8090}

spring:
  application:
    name: reparacion-service

  datasource:
    url: ${DB_URL:jdbc:postgresql://ep-red-river-aq8u3sa8-pooler.c-8.us-east-1.aws.neon.tech/apr_reparacion_db?sslmode=require&channel_binding=require}
    username: ${DB_USERNAME:neondb_owner}
    password: ${DB_PASSWORD:npg_hFv5nmcI7QDC}

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

springdoc:
  api-docs:
    path: /v3/api-docs
  swagger-ui:
    path: /swagger-ui.html

eureka:
  client:
    service-url:
      defaultZone: ${EUREKA_URL:http://localhost:8761/eureka}
  instance:
    prefer-ip-address: true

service:
  incidencia:
    url: ${SERVICE_INCIDENCIA_URL:http://incidencia-service}
