server:
  port: 8090

spring:
  application:
    name: reparacion-service

  datasource:
    url: jdbc:postgresql://ep-red-river-aq8u3sa8-pooler.c-8.us-east-1.aws.neon.tech/apr_reparacion_db?sslmode=require&channel_binding=require
    username: neondb_owner
    password: npg_hFv5nmcI7QDC

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka
  instance:
    prefer-ip-address: true
